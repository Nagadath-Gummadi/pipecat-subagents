- ⚠️ The `@task` decorator now requires a keyword `name` argument and no longer supports the bare `@task` form or a default unnamed handler. Use `@task(name="...")` to register a named task handler; requests without a matching named handler now fall back to `on_task_request`. Existing `@task` (no args) and `@task(name=None)` usages must be updated.
