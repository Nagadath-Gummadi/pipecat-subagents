- Added a `sequential` flag to the `@task` decorator. When `@task(name="...", sequential=True)` is used, concurrent requests for that task name are serialized and run one at a time in FIFO order via a per-name `asyncio.Lock`. The wait time counts against the requester's timeout.
